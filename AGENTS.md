# AGENTS.md

## Gitルール

- コミットメッセージは日本語・Conventional Commits形式で（例: `feat: プロジェクト一覧画面を追加`）
- `git -C` オプションは使用禁止。プッシュは人間がやる

## ドキュメント運用

- 仕様・設計ドキュメントは `docs/` に適宜整理する。全体の地図は `docs/README.md`
- ADRを `docs/adr/` に保存する
- 通常のドキュメントは追記で積み上げず、書き換え・削除で現在の状態を保つ。時系列の経緯はGitやADR、backlogのタスクに任せる

## 実装方針

- コード品質を重視する。破壊的な変更、既存仕様の変更も可能。より良い設計を追求する
- 後方互換性を維持しないこと。互換レイヤーやフォールバック、移行処理の追加はしない。移行が必要なら手動コマンド例をADRに残し、実行はユーザーがやる
- 短期的解決ではなく、長期的視点での解決を優先する。パッチワーク的な修正は禁止
- リスクや開発コストを判断根拠にはせず、適した設計・実装を優先する
- コメントは必要最小限に。経緯の説明をコードコメントに書かない
- フロントエンドのUIを触るときは `docs/design-system.md` の規約を参考に。レイアウトの正は実装

## 検証・テストの運用

- 実装中のループでは変更範囲についてテストを実行する。フルの `pnpm check && pnpm test` はタスク完了時に1回確認する
- テストは網羅性より実行速度。低価値なテストは削ってよい
- 失敗しているテストを既知の問題として放置しない。今直すか、その場で起票する
- ブラウザテスト（`pnpm test:smoke`）はUI・レイアウトに触れた変更のときに実行する。レイアウトに関わる変更ではその結果を受け入れ条件に含める

## マルチエージェント運用

- 統括担当は、要件整理・分解・委任・進行管理・結果統合・差し戻し判断に専念し、原則としてコード編集・テスト実行・ブラウザ操作を行わない（コミット前確認も例外にしない。コミット判断のみ統括が持つ）
- 仕様・要件の確定は統括の仕事。委譲時は具体的に表示内容・文言・既存UIとの整合を決めた仕様を渡す
- 実装担当は実装・修正・テストコード追加を担当する。さらにサブ実装へ分割できるときは、担当範囲であればさらに委任して並列化してよい。成果は取りまとめて統括へ一括報告する
- 検証担当はテストやブラウザ検証を担当し、問題は証拠付きで統括へ報告する。原則、修正しない
- 実装担当は1タスク1worktreeを既定とする。統括が `git worktree add .worktrees/<タスクID>` で専用ブランチのパスを用意して渡す。worktreeごとに `pnpm install` する。統合は統括がマージで行う
- レビュー担当（Sonnet）を常設し、タスク完了ごとにコミット前レビューを挟む。観点は「報告にない副作用」に絞る
- 実装担当は受け入れ条件を満たすたびに `pnpm backlog task edit <id> --check-ac <n>` でチェックを入れ、完了時はテキストで報告する。統括はこれを進捗信号とし、pollingしない
- Claude Codeの場合
  - 総括担当: Fable or Opus
  - 実装担当: Sonnet or Cursor（UI・デザイン系の実装はSonnet、ロジック中心はCursorに）
  - 検証担当: Sonnet
- Codexの場合
  - 総括担当: Sol
  - 実装担当: Terra or Cursor
  - 検証担当: Luna

## デバッグ方法

- ブラウザを使ったデバッグはagent-browserを使う。それ以外は必要なときのみ
- 干渉防止のためagent-browserは `--session <name>` を付けて専用セッションでの操作を推奨
- 開発サーバーはだいたい別のシェルで起動中なので `pnpm dev` を実行せずアクセスしてOK。URLは `http://[<ブランチ名>.]mimi.localhost:1355` (IPアドレスは不可)
- git worktreeの場合 `client/` で `pnpm dev:new-work` を起動して確認する
- 実データDBに対する検証はせず、書き込みはフィクスチャアダプタ（`dev:new-work` 等のモックシナリオ）で検証する

<!-- BACKLOG.MD GUIDELINES START -->

## タスク管理（Backlog.md CLI）

- タスクの参照・作成・更新は `pnpm backlog` CLI で行う。`backlog/` 配下のMarkdown直接編集は禁止（メタデータが壊れる）
- 一覧: `pnpm backlog task list --plain`、詳細: `pnpm backlog task view <id> --plain`、検索: `pnpm backlog search "<語>" --plain`
- タスクの作成・着手・完了の前に、対応するガイドを読む: `pnpm backlog instructions task-creation | task-execution | task-finalization`
- 迷ったら `pnpm backlog <コマンド> --help`
<!-- BACKLOG.MD GUIDELINES END -->
- 残タスク・実装計画・作業メモは backlog のタスクに集約し、HANDOFF や他の docs に残タスクリストを分散させない
- タスクにするのは「検証可能な受け入れ条件が書けて、1〜数PRで完結する見通しがある」ものだけ。要件が未定・ふんわりしたものはドラフト（`pnpm backlog draft`）に置き、着手を決めたらまず「要件を決める」タスクを切る
- 受け入れ条件は7項目程度を上限にする。超えるならタスクを分割する。起票時に確定仕様（ドラフト・ADR）を受け入れ条件へ1行ずつ対応付ける
