[features]
multi_agent = true

[agents]
# 同時実行の総数上限。Sol + 担当エージェント群を並列に動かす。
# 独立サブタスクのファンアウト（実装2体+検証+孫など）を見込んで広めに確保する。
max_threads = 8
# 委任の階層上限。1=統括→担当のみ、2=担当が独立サブ実装をさらに委任可（孫まで）。
# ひ孫以降は追跡性が割に合わないため2で止める。
max_depth = 2
