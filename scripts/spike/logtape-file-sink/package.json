{
  "name": "@mimimilli/spike-logtape-file-sink",
  "private": true,
  "type": "module",
  "scripts": {
    "spike": "bun run src/spike.ts --mode normal",
    "spike:exit": "bun run src/spike.ts --mode exit-immediate",
    "spike:sigint": "bun run src/spike.ts --mode sigint-loop",
    "compile": "bun run src/build-compile.ts",
    "build:windows": "bun run src/build-windows.ts"
  },
  "dependencies": {
    "@logtape/file": "^2.3.0",
    "@logtape/logtape": "^2.3.0"
  }
}
